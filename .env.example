# Copy to `.env` and fill in. Restart the bundler after changing it —
# EXPO_PUBLIC_* values are inlined at build time, not read at runtime.

# Free client id from https://devportal.jamendo.com
# The free tier is non-commercial only and allows 35,000 requests per month.
EXPO_PUBLIC_JAMENDO_CLIENT_ID=
